<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc" 
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc 
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context 
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enable component scanning -->
    <context:component-scan base-package="com.hdims.hdims" />

    <!-- Enable Spring MVC annotations -->
    <mvc:annotation-driven />

    <!-- Configure JSON message converter -->
    <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter" />

    <!-- Configure CORS (optional) -->
    <mvc:interceptors>
        <bean class="com.hdims.hdims.config.CorsInterceptor" />
    </mvc:interceptors>

    <!-- Static resources configuration (optional) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    
    <!-- Exception Handling -->
    <bean class="com.hdims.hdims.exception.GlobalExceptionHandler" />

</beans>
